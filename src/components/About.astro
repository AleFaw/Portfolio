---
// src/components/About.astro
const skills = [
  "PHP", "MySQL", "Laravel", "React", "Astro", "Tailwind", "Git", "Python", "Blender"
];
---

<section id="sobre-mi" class="py-20 px-4 bg-slate-900/50 overflow-hidden">
  <div class="max-w-6xl mx-auto flex flex-col md:flex-row items-center gap-12">
    
    <div class="about-text opacity-0 md:w-1/2">
      <h2 class="text-4xl font-bold text-white mb-6">
        Más allá del <span class="text-blue-500">Código</span>
      </h2>
      
      <p class="text-gray-300 text-lg leading-relaxed mb-6">
        Soy un <strong>Desarrollador Full Stack</strong> con formación en <strong>Ingeniería Mecatrónica</strong>. Esta combinación me permite entender no solo cómo escribir software, sino cómo estructurar soluciones lógicas y escalables.
      </p>

      <p class="text-gray-300 text-lg leading-relaxed mb-8">
        Aunque mi fuerte ha sido el backend (PHP/MySQL), actualmente estoy expandiendo mis horizontes hacia el ecosistema moderno de JavaScript (React, Astro) y el diseño 3D. Me apasiona crear herramientas que la gente realmente disfrute usar.
      </p>

      <div>
        <h3 class="text-white font-semibold mb-4 flex items-center gap-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
          </svg>
          Tecnologías que uso:
        </h3>
        <div class="flex flex-wrap gap-3">
          {skills.map((skill) => (
            <span class="px-4 py-2 bg-slate-800 text-blue-300 rounded-lg text-sm font-medium border border-slate-700 hover:border-blue-500 transition-colors cursor-default">
              {skill}
            </span>
          ))}
        </div>
      </div>
    </div>

    <div class="about-img opacity-0 md:w-1/2 flex justify-center">
      <div class="relative w-64 h-64 md:w-80 md:h-80">
        <div class="absolute inset-0 bg-gradient-to-tr from-blue-600 to-purple-600 rounded-full blur-2xl opacity-50 animate-pulse"></div>
        <div class="relative w-full h-full bg-slate-800 rounded-2xl border border-slate-700 flex items-center justify-center overflow-hidden rotate-3 hover:rotate-0 transition-transform duration-500">
            <span class="text-6xl">👨‍💻</span>
        </div>
      </div>
    </div>

  </div>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  // Animación del Texto (Entra desde la izquierda)
  gsap.fromTo(".about-text",
    { x: -100, opacity: 0 }, // Empieza a la izquierda e invisible
    {
      x: 0, opacity: 1, duration: 1, ease: "power3.out",
      scrollTrigger: {
        trigger: "#sobre-mi",
        start: "top 60%", // Empieza cuando el top de la sección está al 60% de la pantalla
      }
    }
  );

  // Animación de la Imagen (Entra desde la derecha)
  gsap.fromTo(".about-img",
    { x: 100, opacity: 0 }, // Empieza a la derecha e invisible
    {
      x: 0, opacity: 1, duration: 6, ease: "power3.out",
      scrollTrigger: {
        trigger: "#sobre-mi",
        start: "top 60%",
      }
    }
  );
</script>